# =============================================================================
# requirements.yml - Complete dependencies for two-node monitoring setup
# =============================================================================

---
collections:
  - name: prometheus.prometheus
  - name: community.general
  - name: community.grafana
  - name: community.postgresql
  - name: ansible.posix

roles:
  - name: geerlingguy.postgresql